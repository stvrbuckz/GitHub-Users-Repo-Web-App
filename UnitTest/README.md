To run the unit tests, you need to have the following dependencies installed:
- xunit
- xunit.runner.visualstudio
- Moq

To run the tests, you can use the following command in the terminal:
```dotnet test

To run the tests in Visual Studio, you can use the Test Explorer. Test -> Test Explorer.